log_errors = On
display_errors = Off
display_startup_errors = Off
; php -S workers (PHP_CLI_SERVER_WORKERS) do not always inherit stderr.
; Railway captures PID 1, so send PHP fatals there.
error_log = /proc/1/fd/2
