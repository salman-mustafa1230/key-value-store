[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
startCommand = "sh docker/start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
