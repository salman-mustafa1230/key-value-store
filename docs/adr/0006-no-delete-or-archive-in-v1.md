# No delete or Archive in this submission

Keys are immortal for v1. There is no DELETE and no Archive endpoint. The spec did not ask for removal; erasing Versions would destroy as-of reads. A later Archive Version can hide a Key from the current snapshot without rewriting history. That path is documented, not built.
